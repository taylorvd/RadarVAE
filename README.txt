To run data_parser.py:

    ./data_parser.py [path to rosbag] [/depth image rostopic]

This generates training set with 80% of data and test set with remaining 20%

Run ./main.py for training + validation loop
